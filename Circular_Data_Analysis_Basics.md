---
ai_assisted: true
review_status: unverified
last_reviewed:
---

# Circular Data Analysis：圆形数据统计分析基础

## 1. 什么是圆形数据（Circular Data）

圆形数据是指定义在圆形空间上的数据，例如：

-   角度
-   相位（phase）
-   方向
-   周期时间

与普通连续变量不同，圆形数据具有周期性。

例如：

0° = 360°

因此：

10° 和 350°

实际上是接近的两个方向。

普通线性距离：

350 - 10 = 340°

不能反映真实的圆形距离。

------------------------------------------------------------------------

# 2. 为什么普通统计方法不适用于角度？

普通统计假设数据位于一条直线上。

例如：

0 ---------------- 100

数字越大表示位置越向右。

但是角度：

          90°

           |

180° ------+------ 0°

           |
          270°

没有真正的起点和终点。

例如：

10° 和 350°

普通平均：

(10 + 350) / 2 = 180°

但实际上两个方向都接近0°。

因此：

-   普通平均
-   普通方差
-   普通线性回归

不能直接用于圆形数据。

------------------------------------------------------------------------

# 3. 将角度转换为二维坐标

一个角度 θ 可以表示为单位圆上的一个点：

x = cos(θ)

y = sin(θ)

例如：

θ = 40°

转换为：

(x, y) = (cos40°, sin40°)

约：

(0.77, 0.64)

因此：

一个角度变量被表示为二维空间中的一个方向点。

------------------------------------------------------------------------

# 4. 为什么使用二维表示？

二维表示保留了圆形数据的重要性质。

因为：

x² + y² = 1

所有数据都位于单位圆上。

因此：

0° 和 360°

会自动成为同一个位置。

10° 和 350°

在二维空间中也是接近的。

这种表示方式称为：

Cartesian embedding（笛卡尔坐标嵌入）。

------------------------------------------------------------------------

# 5. 为什么不是两个独立变量？

虽然转换以后有：

x 坐标

y 坐标

但是它们不是两个独立变量。

原因：

它们受到单位圆约束：

x² + y² = 1

因此：

(x,y)

共同表示一个方向。

它类似于：

地图中的二维坐标表示一个位置。

不是两个独立的位置变量。

------------------------------------------------------------------------

# 6. 二维联合分布（Bivariate Joint Distribution）

联合分布描述多个变量同时出现的概率。

一维：

P(X)

表示一个变量的概率分布。

二维：

P(X,Y)

表示两个变量组合的概率分布。

例如：

身高和体重：

P(height, weight)

描述二者之间的联合关系。

------------------------------------------------------------------------

# 7. 圆形数据中的二维联合分布

角度转换后：

每个观测变成：

(cosθ, sinθ)

大量观测形成二维点云。

二维分布描述：

-   数据中心在哪里；
-   数据如何扩散；
-   方向是否集中。

例如：

如果角度集中在40°：

二维点会聚集在40°方向。

如果角度随机：

二维点会均匀分布在圆周附近。

------------------------------------------------------------------------

# 8. 二维正态分布（Bivariate Normal Distribution）

二维正态分布：

(X,Y) \~ N(μ,Σ)

描述二维空间中的概率云。

它包含两个核心部分：

## 均值向量 μ

表示分布中心。

例如：

二维点云中心位置。

## 协方差矩阵 Σ

表示数据扩散方式。

包括：

-   X方向变化；
-   Y方向变化；
-   两个方向之间的相关性。

------------------------------------------------------------------------

# 9. Projected Normal Distribution（投影正态分布）

Projected Normal 的思想：

不直接在圆上建立模型。

而是假设：

第一步：

数据来自二维正态分布：

(X,Y) \~ N(μ,Σ)

第二步：

将二维点投影到圆上。

即：

从原点连接二维点。

这个方向对应一个角度：

θ

因此：

二维正态分布产生圆形方向分布。

------------------------------------------------------------------------

# 10. Circular Regression（圆形回归）

普通回归研究：

某个因素如何改变一个数值。

例如：

X 是否影响 Y。

圆形回归研究：

某个因素是否改变方向分布。

例如：

条件A：

40°

条件B：

100°

模型关注：

方向是否发生系统旋转。

------------------------------------------------------------------------

# 11. Circular Mixed Model（圆形混合模型）

很多实验数据具有层级结构。

例如：

多个观测来自同一个被试。

普通混合模型：

Y = 固定效应 + 被试随机效应 + 误差

圆形混合模型：

方向分布 =

固定效应

-   

个体随机方向偏移

-   

误差

随机效应允许：

不同个体具有不同的baseline方向。

------------------------------------------------------------------------

# 12. 圆形模型中的效应解释

线性模型：

效应量通常表示：

增加多少单位。

例如：

+5 cm。

圆形模型：

效应通常表现为：

## 方向旋转

例如：

preferred direction 从40°变化到80°。

## 集中程度变化

例如：

方向分布变得更加集中或分散。

------------------------------------------------------------------------

# 13. 常见圆形统计模型

## Von Mises Distribution

圆形数据中的经典分布。

类似于线性空间中的正态分布。

## Projected Normal Distribution

优点：

可以自然扩展到：

-   回归模型；
-   混合模型；
-   贝叶斯层级模型。

## Bayesian Circular Mixed Model

适用于：

重复测量和复杂实验设计。

------------------------------------------------------------------------

# 14. 核心总结

圆形数据分析的核心思想：

1.  角度不是线性变量，而是方向变量。

2.  使用：

(cosθ, sinθ)

将方向表示为二维空间中的点。

3.  二维表示不是增加两个变量，而是改变表示方式。

4.  二维联合分布用于描述方向数据的集中和变化。

5.  Projected Normal 模型利用二维概率分布建立圆形模型。

6.  Circular mixed model 用于研究：

某些因素是否导致方向分布发生系统变化。
