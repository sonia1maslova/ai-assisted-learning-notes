---
ai_assisted: true
review_status: unverified
last_reviewed:
---

# 圆周分布聚合与复向量平均的数学等价性

> 本文中的公式使用 Unicode 字符和纯文本公式块书写，不依赖 LaTeX、MathJax 或其他公式插件。

## 1. 问题

对于一组定义在圆周上的离散分布，可以用两种方式求集体偏好方向：

1. 先平均归一化分布，再计算偏好方向。
2. 先把每个归一化分布转成复向量，再平均复向量。

只要两种方法使用相同的有效样本、分箱中心和聚合权重，它们在数学上完全等价。18 个分箱只是常见实例；下面对任意分箱数 K 都成立。

---

## 2. 记号

假设有 N 个样本，每个样本都有 K 个圆周分箱。

- i = 1, 2, …, N：样本索引。
- k = 1, 2, …, K：分箱索引。
- a(i,k)：样本 i 在分箱 k 中的非负原始值。
- θ(k)：分箱 k 的中心角度。
- j：虚数单位，j² = −1。

样本 i 的原始总量为：

```text
S(i) = Σ[k=1…K] a(i,k)       且 S(i) > 0
```

对每个样本分别归一化：

```text
p(i,k) = a(i,k) / S(i)
```

因此，每个样本的归一化分布之和为 1：

```text
Σ[k=1…K] p(i,k) = 1
```

样本 i 的复向量定义为：

```text
z(i) = Σ[k=1…K] p(i,k) · exp(jθ(k))
```

也可写成实部和虚部：

```text
z(i) = Σ[k=1…K] p(i,k) · cos(θ(k))
     + j · Σ[k=1…K] p(i,k) · sin(θ(k))
```

其中：

- `arg(z(i))` 是该样本的偏好方向。
- `|z(i)|` 是该分布的方向集中度。
- 如果 `|z(i)| = 0`，偏好方向未定义。

---

## 3. 方法 A：先平均分布

首先对 N 个已归一化的分布逐分箱平均：

```text
p̄(k) = (1/N) · Σ[i=1…N] p(i,k)
```

平均分布仍然是归一化分布：

```text
Σ[k=1…K] p̄(k)
= (1/N) · Σ[i=1…N] Σ[k=1…K] p(i,k)
= (1/N) · N
= 1
```

因此，通常不需要再对平均分布归一化。

由平均分布得到集体复向量：

```text
z(A) = Σ[k=1…K] p̄(k) · exp(jθ(k))
```

集体偏好方向和集中度为：

```text
μ(A) = arg(z(A))
R(A) = |z(A)|
```

---

## 4. 方法 B：先计算每个分布的复向量

先把每个归一化分布转换为复向量：

```text
z(i) = Σ[k=1…K] p(i,k) · exp(jθ(k))
```

再对 N 个复向量等权平均：

```text
z(B) = (1/N) · Σ[i=1…N] z(i)
```

集体偏好方向和集中度为：

```text
μ(B) = arg(z(B))
R(B) = |z(B)|
```

这里平均的是完整复向量，不是对偏好角度做普通算术平均。

---

## 5. 两种方法的等价性证明

将方法 A 中的平均分布展开：

```text
z(A)
= Σ[k=1…K] p̄(k) · exp(jθ(k))

= Σ[k=1…K] [(1/N) · Σ[i=1…N] p(i,k)] · exp(jθ(k))

= (1/N) · Σ[i=1…N] Σ[k=1…K] p(i,k) · exp(jθ(k))

= (1/N) · Σ[i=1…N] z(i)

= z(B)
```

第三行只是交换了两个有限求和的顺序。从分布到复向量的转换：

```text
(p(1), p(2), …, p(K)) → Σ[k=1…K] p(k) · exp(jθ(k))
```

是线性运算，所以它和平均运算可以交换顺序。

因为：

```text
z(A) = z(B)
```

所以两种方法得到的整个复向量相同，不仅是偏好方向相同：

```text
μ(A) = μ(B)
R(A) = R(B)
```

如果聚合向量恰好为零，两种方法都得到零向量，偏好方向也都未定义。

---

## 6. 直观解释

每个分箱中心都对应一个圆周上的单位向量：

```text
exp(jθ(k)) = cos(θ(k)) + j · sin(θ(k))
```

`p(i,k)` 是样本 i 给第 k 个单位向量的权重。“先平均每个分箱的权重”与“先按各自权重得到向量，再平均向量”只是改变了加法顺序，所以结果不变。

---

## 7. 加权平均的推广

设样本 i 的聚合权重是 w(i)：

```text
w(i) ≥ 0
Σ[i=1…N] w(i) = 1
```

如果两种方法使用完全相同的 w(i)，则：

```text
p̄w(k) = Σ[i=1…N] w(i) · p(i,k)

z(A,w)
= Σ[k=1…K] p̄w(k) · exp(jθ(k))
= Σ[i=1…N] w(i) · Σ[k=1…K] p(i,k) · exp(jθ(k))
= Σ[i=1…N] w(i) · z(i)
= z(B,w)
```

因此，关键不是必须等权，而是两种方法必须使用同一组权重。

---

## 8. 分层聚合

数据可能存在多个层级：

```text
基础样本 → 组内平均 → 个体平均 → 群体平均
```

可以在每一层平均归一化分布，也可以在每一层平均复向量。只要每一层使用相同的分组、有效样本和权重，两条路径仍然等价。

但是，分层等权平均通常不等于把所有底层样本平铺后一次性平均。先对每组内的基础样本等权平均，再对各组等权平均，意味着每个组获得相同总权重。当各组样本数不同时，底层样本的最终隐含权重也不同。

---

## 9. 不等价的常见情况

### 9.1 先平均原始分布，最后只归一化一次

如果不对每个样本先归一化，而是计算：

```text
p̃(k) = Σ[i=1…N] a(i,k) / Σ[i=1…N] Σ[k=1…K] a(i,k)
```

它等于：

```text
p̃(k) = Σ[i=1…N] { S(i) / Σ[r=1…N] S(r) } · p(i,k)
```

这不是对各归一化分布等权平均，而是按样本总量 S(i) 加权。总量越大的样本对结果影响越大。

### 9.2 直接对偏好角度做算术平均

角度是周期变量。例如，179° 和 −179° 的平均方向接近 180°，但普通算术平均会得到 0°。应平均复向量，而不是直接平均角度。

### 9.3 平均前把每个复向量强制单位化

如果把 `z(i)` 替换为：

```text
z(i) / |z(i)|
```

就会保留每个样本的方向，但删除其自身的集中度。这改变了统计目标，不再等价于平均原始的归一化分布。

### 9.4 聚合前执行非线性变换

阈值化、只保留最大分箱、向量单位化或对向量长度非线性缩放，都会使“变换”和“平均”不再能自由交换顺序。

### 9.5 有效样本或缺失值处理不同

如果两种方法使用不同的样本集，或对空分箱、非有限值的排除规则不同，则结果不会严格相同。

---

## 10. 等价性检查清单

1. 每个样本都在聚合前单独归一化。
2. 所有样本使用相同的分箱边界和分箱中心。
3. 两种方法使用相同的有效样本集。
4. 两种方法使用相同的样本权重。
5. 如果存在分层聚合，每层的分组、有效样本和权重都一致。
6. 两种方法都使用同一个线性圆周向量映射。
7. 聚合前没有额外将复向量单位化。
8. 没有用普通角度算术平均代替复向量平均。

---

## 11. 结论

```text
路径 A：
每个样本分别归一化
    → 平均归一化分布
    → 转换为复向量
    → 获得偏好方向

路径 B：
每个样本分别归一化
    → 每个分布分别转换为复向量
    → 平均复向量
    → 获得偏好方向
```

从归一化圆周分布到复向量的转换是线性运算，因此它与使用相同权重的平均运算可以交换顺序。在条件一致时，两种方法会得到完全相同的复向量，因而得到相同的集体偏好方向和相同的向量长度。

